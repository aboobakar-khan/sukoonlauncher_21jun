<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- Location for prayer times (GPS + network) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    
    <!-- Screen Time Analytics - Usage Stats Permission -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    
    <!-- App Blocker Service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Zen Mode: overlay to block notification bar pull-down -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Pull-down notification panel from swipe gesture -->
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />
    
    <!-- Zen Mode: Do Not Disturb control (must be declared to appear in DND access list) -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Prayer Alarm: exact alarm scheduling (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Prayer Alarm: vibrate for notification -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Prayer Alarm: wake device for alarm sound -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Prayer Alarm: request battery optimization exemption (Samsung/Xiaomi/etc.) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Custom audio: read audio files from device storage (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Custom audio: read storage for Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- Prayer Alarm: full-screen intent shown on lock screen (Android 14+) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:label="Sukoon"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:enableOnBackInvokedCallback="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="false">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- LAUNCHER MODE: Makes this app appear as a Home Screen option -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        
        <!-- App Blocker foreground service -->
        <service
            android:name=".AppBlockerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />
        
        <!-- Blocking overlay screen (covers blocked apps) -->
        <activity
            android:name=".BlockedAppActivity"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:taskAffinity="com.sukoon.launcher.blocker" />
        
        <!-- Restart blocker service after device reboot -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Timer Alarm: exact alarm fires when app timer deadline is reached -->
        <receiver
            android:name=".TimerAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.sukoon.launcher.TIMER_EXPIRED" />
            </intent-filter>
        </receiver>

        <!-- Prayer Alarm: android_alarm_manager_plus broadcast receiver + service -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>


        <!-- Zen Mode: lock-screen overlay Activity shown over keyguard during Zen session -->
        <!-- Uses FLAG_SHOW_WHEN_LOCKED so the Zen screen appears without unlocking -->
        <activity
            android:name=".ZenLockScreenActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:screenOrientation="portrait"
            android:taskAffinity="com.sukoon.launcher.zen"
            android:launchMode="singleTop" />

        <!-- Prayer Alarm: full-screen lock-screen UI shown when adhan fires -->
        <!-- FLAG_SHOW_WHEN_LOCKED + FLAG_TURN_SCREEN_ON applied programmatically -->
        <activity
            android:name=".SalahAlarmActivity"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:taskAffinity="com.sukoon.launcher.alarm" />

        <!-- Prayer Alarm: BroadcastReceiver that shows notification + plays adhan -->
        <receiver
            android:name=".AlarmBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.sukoon.launcher.PRAYER_ALARM" />
                <action android:name="com.sukoon.launcher.DISMISS_ADHAN" />
            </intent-filter>
        </receiver>

        <!-- Device Admin: required for double-tap lock screen (dpm.lockNow()) -->
        <receiver
            android:name=".SukoonDeviceAdmin"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policy" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Notification Filter: Captures notifications from all apps for the in-launcher feed -->
        <service
            android:name=".SukoonNotificationListenerService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- App Timer: precise, instant foreground-app detection (window-state only) -->
        <service
            android:name=".SukoonAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
    <queries>
        <!-- Launcher: query all apps with a launcher activity (replaces QUERY_ALL_PACKAGES) -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Quick Actions: auto-detect phone dialer -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <!-- Quick Actions: auto-detect camera -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_GALLERY" />
        </intent>
        <!-- Text processing -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Google Pay packages for quick launch -->
        <package android:name="com.google.android.apps.nbu.paisa.user" />
        <package android:name="com.google.android.apps.pay" />
        <package android:name="com.google.android.apps.walletnfchost" />
    </queries>
</manifest>
